# stack.yml for Admin
version: "3.8"

services:
  app:
    image: "{{ image }}"
    networks:
      - dueloo
      - "{{ traefik_network }}"
    deploy:
      mode: replicated
      replicas: 1
      update_config:
        order: start-first
        parallelism: 1
        failure_action: rollback
      rollback_config:
        order: start-first
        parallelism: 1
      restart_policy:
        condition: on-failure
      placement:
        constraints:
          - node.role == manager
      labels:
        - traefik.enable=true
        - traefik.constraint-label=traefik-public
        # Http router: redirection to https
        - traefik.http.routers.{{ env }}-dueloo_admin-http.rule=Host(`{{ hostname }}`)
        - traefik.http.routers.{{ env }}-dueloo_admin-http.entrypoints=http
        - traefik.http.routers.{{ env }}-dueloo_admin-http.middlewares=https-redirect
        # Https router
        - traefik.http.routers.{{ env }}-dueloo_admin-https.rule=Host(`{{ hostname }}`)
        - traefik.http.routers.{{ env }}-dueloo_admin-https.entrypoints=https
        - traefik.http.routers.{{ env }}-dueloo_admin-https.tls=true
        - traefik.http.routers.{{ env }}-dueloo_admin-https.tls.certresolver=letsencrypt
        # Pointing to app port
        - traefik.http.services.{{ env }}-dueloo_admin.loadbalancer.server.port=80

networks:
  dueloo:
    name: "{{ env }}-dueloo-admin"
  "{{ traefik_network }}":
    external: true
